package org.example;
import org.example.command.CommandBuilder;

public class JuliaFractalApp {
    public static void main(String[] args) {
        CommandBuilder commandBuilder = new CommandBuilder();
        commandBuilder.getManual();
    }
}